# Dependencies
node_modules/

# Build output
dist/

# Environment
.env
.env.local

# OAuth token storage (legacy, now in .taskagent/token.json)

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# TaskAgent internal files
# The .taskagent/ directory structure is tracked, but runtime files are ignored
.taskagent/queue.db
.taskagent/queue.db-wal
.taskagent/queue.db-shm
.taskagent/token.json
.taskagent/cache/
.taskagent/worktrees/
.taskagent/cached-prompts/

# Legacy paths (will be auto-migrated on first run)
.task-agent/
.task-agent-queue.db
.task-agent-queue.db-wal
.task-agent-queue.db-shm
.task-agent.db
.task-agent-repo-summary.json
.task-agent-token.json

# Beads issue tracking (local only)
.beads/
